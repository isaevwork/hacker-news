import React, { type FC } from 'react'import classes from './ArticleContainer.module.css'import { unixToDate } from '../../../utils/utils'import { Link } from 'react-router-dom'interface ArticleContainerProps {  id: number  title: string  userName: string  rating: number  time: number}const ArticleContainer: FC<ArticleContainerProps> = (props) => {  const { id, title, userName, rating, time } = props  const ratingClassArr = [classes.rating]  if (rating > 50) {    ratingClassArr.push(classes.highRating)  } else if (rating > 30) {    ratingClassArr.push(classes.middleRating)  } else {    ratingClassArr.push(classes.lowRating)  }  return (      <div className={`${classes.articleWrapper}`}>          <div>              <Link className={classes.link} to={`comments/${id}`} >                  <h1>{title}</h1>              </Link>              <div className={classes.infoWrapper}>                  <span>{userName}</span>                  <span>{unixToDate(time)}</span>              </div>          </div>          <div className={classes.ratingWrapper}>              <span className={ratingClassArr.join(' ')}>{`${rating} очков`}</span>          </div>      </div>  )}export default ArticleContainer