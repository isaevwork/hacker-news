import ArticleContainer from './ArticleContainer'import classes from './Article.module.css'import { useEffect, useState } from 'react'import { get } from '../../../api/api'import { type ArticleFormat } from '../../types/types'const Article = () => {  const [articles, setArticles] = useState<ArticleFormat[]>([])  useEffect(() => {    void getNewList()  }, [])  async function getNewList () {    const newsIds = await get('https://hacker-news.firebaseio.com/v0/topstories.json?print=pretty&print=pretty&orderBy="$key"&limitToFirst=30')    const newsList = await Promise      .all(newsIds.map(async (id: string) => await get(`https://hacker-news.firebaseio.com/v0/item/${id}.json?print=pretty`)))    setArticles(newsList)  }  return (      <div className={classes.articleWrapper}>          {articles.map((note: ArticleFormat) => (              <ArticleContainer id={note.id} title={note.title} userName={note.by} rating={note.score} time={note.time} key={note.id}/>          ))}      </div>  )}export default Article