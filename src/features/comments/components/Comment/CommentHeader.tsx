import React, { type FC } from 'react'import classes from './CommentHeader.module.css'import { domainToHostname, openUrl, unixToDate } from '../../../../utils/utils'interface CommentHeaderProps {  title: string  name: string  date: number  url: string}const CommentHeader: FC<CommentHeaderProps> = (props) => {  const { title, name, date, url } = props  return (      <div className={classes.CommentHeaderWrapper}>          <span>{title}</span>          <div className={classes.infoBlock}>              <div className={classes.subTitle}>                  <span>{name}</span>                  <span>{unixToDate(date)}</span>              </div>              <div onClick={() => { openUrl(url) }} className={classes.link}>{domainToHostname(url)}</div>          </div>      </div>  )}export default CommentHeader